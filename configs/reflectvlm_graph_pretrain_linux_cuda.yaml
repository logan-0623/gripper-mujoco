dataset:
  repo_id: yunhaif/ReflectVLM-data-expert
  source_split: train
  split_seed: 0
  split_ratios: [0.8, 0.1, 0.1]
  max_rows: null

model:
  image_size: 128
  max_history_tokens: 64
  image_embedding_dim: 128
  text_embedding_dim: 64
  graph_embedding_dim: 128

training:
  output_dir: outputs/graph_pretrain/reflectvlm_cuda
  device: cuda
  batch_size: 16
  num_workers: 0
  epochs: 20
  learning_rate: 0.0003
  weight_decay: 0.0001
  seed: 0
